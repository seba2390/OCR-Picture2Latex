\begin{align}\calL(\alpha, \beta) &= \sum_{\yy \in \calC} \log \left( \frac{(\alpha\cdot l(\yy)^\beta)^k}{k!}\exp(-\alpha\cdot l(\yy)^\beta) \right)\\ &= \sum_{\yy \in \calC} k \log (\alpha\cdot l(\yy)^\beta) - \log(k!) -\alpha\cdot l(\yy)^\beta \\ &= \sum_{\yy \in \calC} k \left (\log(\alpha) + \beta\cdot\log (l(\yy)) \right)- \log(k!) -\alpha\cdot l(\yy)^\beta\end{align}