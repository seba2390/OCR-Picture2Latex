\begin{equation}\begin{aligned} fit(\calS_{est}, \calS_{gt}) &= \frac{\vert \calS_{close} \vert}{\vert \calS_{est} \vert}, \\ \calS_{close} &= \{\bfv \in \calS_{est}: d_f(\bfv, \calS_{gt}) < \lambda \}, \end{aligned}\end{equation}