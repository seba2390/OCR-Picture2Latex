\begin{equation}\begin{split} \frac{a} {\sqrt{\bc^T\widehat{{\bTheta}}\bc}} &= \frac{a}{\sigma} \cdot \frac{\sigma}{\sqrt{\bc^T\widehat{{\bTheta}}\bc}} = \frac{a}{\sigma}\left(1+ \frac{\sigma- \sqrt{\bc^T\widehat{{\bTheta}}\bc}} {\sqrt{\bc^T\widehat{{\bTheta}}\bc}} \right) \\ &= \frac{a}{\sigma} \left(1+ \frac{\sigma^2- \bc^T\widehat{{\bTheta}}\bc} {\sqrt{\bc^T\widehat{{\bTheta}}\bc} \left( \sigma+\sqrt{\bc^T\widehat{{\bTheta}}\bc}\right) } \right). \end{split}\end{equation}