\usetikzlibrary{positioning}

%\definecolor{updateD}{Hsb}{100, 0.81, 0.76}
\definecolor{updateF}{Hsb}{0, 0.4, 1.0}

\definecolor{linvD}{Hsb}{203, 1.0, 0.8}
\definecolor{linvF}{Hsb}{203, 0.4, 0.8}

\tikzset{
	%shapes: star (star point ratio)
	% regular polygon, regular polygon sides=x
	every picture/.style={
		node distance=1.5cm and 1.5cm,% and 1.5cm,
		on grid=true,
		%framed,
		semithick,
		>=latex,
		%>=to,%>=latex % arrow tip style
		%bend angle=5,
		auto, % placement of edge labels
		default/.style={
			circle,
			%minimum size=6.5mm,% previously 6mm
			very thick,
			draw= none,
			fill= lightgray,
		},
		rect/.style={
			rectangle,
			rounded corners,
			%minimum size=6.5mm,% previously 6mm
			very thick,
			draw= none,
			fill= lightgray,
		},
		linv/.style={% loop invariant node style
			rectangle,
			rounded corners,
			%minimum size=6.5mm,% previously 6mm
			very thick,
			draw= none,
			fill= linvF,
		},
		update/.style={
			rectangle,
			rounded corners,
			%minimum size=6.5mm,% previously 6mm
			very thick,
			draw= none,
			fill= updateF,
		},
%		internal/.style={
%			circle,
%			minimum size=6.5mm,% previously 6mm
%			very thick,
%			draw= blue,
%			fill= blue,
%		},
	},
	every matrix/.style={
		%column sep= {8cm,between origins},
		row sep=1cm,
		column sep= {0.4cm},
	}
}
