\MYnote{
\begin{algorithm}
\caption{Greedy Method}
\begin{algorithmic}[1]
\State $\textit{update\_queue = {initialize with set of Model_{ij}}}$
\While{$\textit{update\_queue is not empty}$}
\IF{$\textit{updated Model_ij is available}$}
\State $\textit{update_queue = {Append all updated Model_ij}}$
\EndIf
\IF{$\textit{System specific parameters look fine}$}
\State $\textit{Choose 'K' models for synchronous update}$
\State $\textit{Assign worker_i to update Model_ij}$
\EndIf
\ELSE 
\State $\textit{Delay update task till system performance improves}$
\EndElse
\EndWhile
\end{algorithmic}
\end{algorithm}
}


second onee


\begin{algorithm}
\caption{DSOC method}
\begin{algorithmic}[2]
\State $\textit{update\_queue = {initialize with set of Model_{ij}}}$
\While{$\textit{update\_queue is not empty}$}
\IF{$\textit{updated Model_ij is available}$}
\State $\textit{update_queue = {Append all updated Model_ij}}$
\EndIf

\State $\textit{assign_priority(M, system params, Application params}$
\end{algorithmic}
\end{algorithm}


%%%% Main calculation equation

% Function assign_priority()
\begin{algorithm}
 \caption{Prioritizing application update}
  \begin{algorithmic}[1]
   \Function{Assign Priority}{sParams,sWeights,aParams,aWeights}  % try to work on these terminologies...
   %\State $\text{System_Perf_value = a1 * (c_ij/(c_free + c_i)) + a2 * (m_ij/(m_free + m_i)) + a3 * (s_ij/(s_free + s_i)) + a4 * (t_ij/ sum(ti)) }$
   
   % Need to write a similar Equation for applicatio specific parameters also. Look at the equation below...
   
   %\State $\text{ Applic_Perf_value = b1 * (acc_ij-paccij/(accij + paccij)/2) + b2 * (pr_ij-Eprij/(prij + Eprij)/2) + b3 * (lat/Elatij) + b4 * (execution_time/avg execution time of apps in that node)}$
   
   
   
   % cpu util/total cpu ---fine
   % memory-used/total memeory --fine
   % storage used/total storage
   % Bandwidthj/# of containers > thresholdBandwidth
   % tput within node, tputij = tpt/sumof all tputj -- To make sure all apps within a node get fair chance to update their model.
   %\State 
   
   %\State $\text{System_Perf_value = sum of all i Sp_val * weight of that feature}$
   %\State $\text{Application_Perf_value = sum of all i App_val * weight of that feature}$
   % \State $\textit{Overall_Perf_value = alpha * System_Perf + Beta * Application_Perf}$
   \State $\textit{priority} \gets \text\{{Green, Yellow, Blue, Red}\}$
   \EndFunction




\end{algorithmic}
\end{algorithm}

