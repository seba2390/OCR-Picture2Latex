\begin{table}[ht]
    \centering
\caption{PLTL FORMULAS GENERATED BY SIGNATURE SYNTHESIZER [SAT-Core].}
\label{table:eval-dataset-2}
%\rowcolors{1}{gray!25}{white}
%\makebox[\textwidth]{
\resizebox{\columnwidth}{!}{
    \begin{tabular}{|l|l|l|}
        \hline
%        \rowcolor{gray!50}    
        \textbf{LTE Attacks}  & \textbf{Synthesized Sigantures [core-SAT]} & \textbf{Attack Behavior}\\ \hline
        Numb Attack           &  $\globally((\mathsf{authentication\_reject} \Rightarrow (\mathsf{authentication\_reject} \since\mathsf{authentication\_response})))$ & It is always the case that authentication response precedes authentication reject. \\ \hline
        Paging with IMSI      &  $\globally(\neg((\mathsf{paging\_IMSI\_and\_TMSI}\vee\mathsf{paging\_IMSI})))$ & It is always the case that no message is passed with IMSI or TMSI. \\ \hline
        Null Encryption       &  $\globally(\neg(\mathsf{MME\_null\_encryption\_chosen})))$ & Null encryption or null integrity are selected even if the UE supports other encryption algorithms.\\ \hline
        RLF Report            &  $\globally((\mathsf{ueInformationResponse\textnormal{-}r9}\Rightarrow((\mathsf{rrcConnectionRelease}\Rightarrow\mathsf{ueInformationResponse\textnormal{-}r9})\since\mathsf{securityModeComplete})))$ & ?\\ \hline
        RLF Report            &  $\globally((\mathsf{ueInformationResponse\textnormal{-}r9}\Rightarrow(\neg (ueCapabilityInformation) \since\mathsf{securityModeComplete})))$ & ?\\ \hline  
        RLF Report            &  $\globally((\mathsf{ueInformationResponse\textnormal{-}r9}\Rightarrow(\neg (ueCapabilityInformation) \since\mathsf{securityModeCommand})))$ & ?\\ \hline  
        RLF Report            &  $\globally((\mathsf{ueInformationResponse\textnormal{-}r9}\Rightarrow(\neg (ueCapabilityEnquiry) \since\mathsf{securityModeCommand})))$ & ?\\ \hline  
        Malformed Identity    & $\globally(\neg(\mathsf{identity\_request\_not\_well\_formed})))$ & ?\\ \hline
        IMSI Catching         &  $\globally(\neg(\mathsf{identity\_request\_IMSI})))$ & ?\\ \hline
        Measurement Report    &  None & None \\ \hline
        Aka Bypass            & $\globally((\mathsf{rrcConnectionReconfiguration}\Rightarrow((\mathsf{rrcConnectionRequest}\Rightarrow\mathsf{securityModeComplete})\since\mathsf{securityModeComplete})))$ & ?\\ \hline
        Aka Bypass            & $\globally((\mathsf{rrcConnectionReconfiguration}\Rightarrow(\neg(\mathsf{rrcConnectionRelease})\since\mathsf{securityModeCommand})))$ & RRC Connection reconfiguration occurs before security mode command procedure. \\ \hline
        IMSI Cracking         &  $\globally(\neg(\mathsf{paging\_IMSI\_and\_TMSI})))$ & No paging with IMSI/TMSI. \\ \hline
    \end{tabular}        
    }
\end{table}

